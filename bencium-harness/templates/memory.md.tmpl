# Project memory

Hot context. Loaded into the agent at the start of every session by the `context-loader` skill. Keep under 100 lines — older items get demoted to `.harness/archive/` via `/bencium-promote`.

## Project

- **Name:** {{product_name}}
- **Killer feature:** {{killer_feature}}
- **Stack:** {{stack}}
- **Deploy:** {{deploy_target}}
- **Initialized:** {{init_timestamp}}

## Decisions

(none yet — use `/bencium-decide` to log)

## Learnings

(none yet — populated by `/bencium-retro`)

## Sprint

(none yet — populated by `/bencium-promote` and `/bencium-feature`)

## Deploys

(none yet — populated by `/bencium-deploy` and `/bencium-rollback`)

## Verifications

(none yet — populated by `/bencium-verify`)

## Roadmap changes

(none yet — populated by `/bencium-feature`)
