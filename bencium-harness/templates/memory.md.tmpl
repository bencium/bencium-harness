# Project memory

Hot context. Loaded into the agent at the start of every session by the `context-loader` skill. Keep under 100 lines — older items get demoted to `.harness/archive/` via `/bencium-promote`.

## Project

- **Name:** {{product_name}}
- **Killer feature:** {{killer_feature}}
- **Stack:** {{stack}}
- **Deploy:** {{deploy_target}}
- **Initialized:** {{init_timestamp}}

## Session handoff

_Volatile "where we are right now" — overwrite it (never append) before `/clear`, compaction, or session end so the next session resumes cleanly. Durable facts belong in `## Decisions` / `## Learnings`, not here. `context-loader` reads this first on SessionStart. Keep the five fields below — fill them in, don't replace them with prose._

- **Active task:** (none yet)
- **Phase:** (none) — plan · build · test · deploy · smoke · reflect
- **Up next:** —
- **Blockers:** none
- **Uncommitted:** clean

## Decisions

(none yet — use `/bencium-decide` to log)

## Learnings

(none yet — populated by `/bencium-retro`)

## Sprint

(none yet — populated by `/bencium-promote` and `/bencium-feature`)

## Deploys

(none yet — populated by `/bencium-deploy` and `/bencium-rollback`)

## Verifications

(none yet — populated by `/bencium-verify`)

## Roadmap changes

(none yet — populated by `/bencium-feature`)
